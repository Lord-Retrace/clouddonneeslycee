<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>Mes projets de codages</title>

    <style>
        body {
            margin:0;
            overflow:hidden;
        }
        #scene {
            position:fixed;
            top:0;
            left:0;
            z-index:-1;
        }
    </style>
</head>

<body>

    <canvas id="scene"></canvas>

    <p style="font-size: 60px;"><strong>Mes Projets de codage</strong></p>
    <p style="font-size: 40px;"><strong>Un site de Ethan Dohan 2nd6</strong></p>

    <a href="mon_autre_page.html">Cliquez ici pour vous rendre sur mon autre page</a>
    <img src="therock.png" alt="le meme de the rock">

    <form>Met ici ton numéro de carte bancaire (blague) <input type="text"></form>

<script type="module">
import * as THREE from "https://cdn.jsdelivr.net/npm/three@0.161.0/build/three.module.js";
import { OBJLoader } from "https://cdn.jsdelivr.net/npm/three@0.161.0/examples/jsm/loaders/OBJLoader.js";

const canvas = document.getElementById("scene");
const renderer = new THREE.WebGLRenderer({ canvas });
renderer.setSize(window.innerWidth, window.innerHeight);

const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, window.innerWidth/window.innerHeight, 0.1, 1000);
camera.position.z = 5;

const light = new THREE.DirectionalLight(0xffffff, 1);
light.position.set(1, 1, 1);
scene.add(light);

const loader = new OBJLoader();
loader.load(
    "stylo.obj",
    function(object){
        object.scale.set(0.1, 0.1, 0.1);
        object.traverse(c => { if(c.isMesh) c.material = new THREE.MeshNormalMaterial(); });
        scene.add(object);
    }
);

function animate() {
    requestAnimationFrame(animate);
    renderer.render(scene, camera);
}
animate();
</script>



</body>
</html>
